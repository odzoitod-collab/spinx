# 🚀 Руководство по деплою на Vercel

## 📋 Подготовка завершена

Сайт полностью подготовлен к деплою на Vercel со всеми необходимыми конфигурациями:

### ✅ Созданные файлы:
- `vercel.json` - конфигурация Vercel
- `README.md` - документация проекта
- `.gitignore` - исключения для Git
- `.env.example` - пример переменных окружения
- `public/favicon.svg` - иконка сайта
- Обновленный `vite.config.ts` для продакшена
- Обновленный `package.json` с правильными зависимостями
- Улучшенный `index.html` с мета-тегами и оптимизацией

### ✅ Проверка сборки:
- Сборка прошла успешно ✓
- Размер бандла оптимизирован ✓
- Все зависимости установлены ✓

## 🚀 Деплой на Vercel

### Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. **Перейдите на [vercel.com](https://vercel.com)**
2. **Войдите в аккаунт** (GitHub/GitLab/Bitbucket)
3. **Нажмите "New Project"**
4. **Импортируйте репозиторий** или загрузите папку
5. **Настройте переменные окружения:**
   ```
   VITE_SUPABASE_URL = https://znpsiawxdtcjyalfkmqn.supabase.co
   VITE_SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpucHNpYXd4ZHRjanlhbGZrbXFuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njc5MTI4OTQsImV4cCI6MjA4MzQ4ODg5NH0.lMULIraQMbWerCyyCpFznMud6nXfE8pQ2xiMIAZiYhg
   VITE_BOT_USERNAME = SpinX_TONBot
   ```
6. **Нажмите "Deploy"**

### Вариант 2: Через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в аккаунт:**
   ```bash
   vercel login
   ```

3. **Перейдите в папку проекта:**
   ```bash
   cd royal-fortune-casino
   ```

4. **Запустите деплой:**
   ```bash
   vercel --prod
   ```

5. **Следуйте инструкциям CLI**

### Вариант 3: Через Git (Автоматический деплой)

1. **Создайте репозиторий на GitHub**
2. **Загрузите код:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/username/royal-fortune-casino.git
   git push -u origin main
   ```
3. **Подключите репозиторий в Vercel Dashboard**
4. **Настройте автоматический деплой при пуше**

## ⚙️ Настройка переменных окружения в Vercel

После деплоя обязательно настройте переменные в Vercel Dashboard:

1. **Перейдите в Settings → Environment Variables**
2. **Добавьте переменные:**
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY` 
   - `VITE_BOT_USERNAME`
3. **Выберите окружения:** Production, Preview, Development
4. **Сохраните изменения**

## 🔧 Настройка домена (Опционально)

1. **В Vercel Dashboard → Settings → Domains**
2. **Добавьте свой домен**
3. **Настройте DNS записи у регистратора**
4. **Дождитесь активации SSL сертификата**

## 🔗 Интеграция с Telegram Bot

После деплоя обновите URL в боте:

1. **Скопируйте URL деплоя** (например: `https://royal-fortune-casino.vercel.app`)
2. **Обновите `WEBSITE_URL` в `bots/casino/config.py`:**
   ```python
   WEBSITE_URL = "https://royal-fortune-casino.vercel.app"
   ```
3. **Перезапустите бота**

## 📱 Тестирование

После деплоя проверьте:
- ✅ Сайт открывается по URL
- ✅ Telegram WebApp интеграция работает
- ✅ Supabase подключение активно
- ✅ Все игры функционируют
- ✅ Кошелек и поддержка работают

## 🚨 Возможные проблемы

### Ошибка сборки
```bash
# Очистите кэш и пересоберите
rm -rf node_modules dist
npm install
npm run build
```

### Проблемы с переменными окружения
- Убедитесь, что все переменные начинаются с `VITE_`
- Проверьте правильность значений в Vercel Dashboard
- Пересоберите проект после изменения переменных

### Telegram WebApp не работает
- Проверьте, что скрипт `telegram-web-app.js` загружается
- Убедитесь, что домен добавлен в настройки бота
- Проверьте HTTPS соединение

## 📊 Мониторинг

Vercel предоставляет встроенную аналитику:
- **Analytics** - статистика посещений
- **Functions** - логи серверных функций  
- **Speed Insights** - производительность
- **Real User Monitoring** - мониторинг пользователей

## 🎉 Готово!

Ваш сайт Royal Fortune Casino готов к использованию на Vercel!

**Следующие шаги:**
1. Протестируйте все функции
2. Обновите URL в Telegram боте
3. Настройте мониторинг и аналитику
4. Добавьте кастомный домен (по желанию)