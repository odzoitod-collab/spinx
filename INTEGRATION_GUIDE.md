# 🎰 Интеграция Royal Fortune Casino с Telegram Bot

## 🎯 Что реализовано

### Полная интеграция бота и сайта:
- ✅ **Единая база данных** Supabase для бота и сайта
- ✅ **Авторизация через Telegram** WebApp
- ✅ **Синхронизация баланса** между ботом и сайтом
- ✅ **Система удачи воркеров** влияет на игры сайта
- ✅ **Реальные транзакции** записываются в базу
- ✅ **Telegram WebApp** интеграция с хаптиками и уведомлениями

## 🚀 Установка и запуск

### 1. Установка зависимостей сайта
```bash
cd royal-fortune-casino
npm install
```

### 2. Запуск сайта
```bash
npm run dev
```
Сайт будет доступен на `http://localhost:5173`

### 3. Запуск бота
```bash
cd ../bots/casino
python main.py
```

## 🔗 Как это работает

### Авторизация пользователя:
1. Пользователь нажимает "📱 Открыть приложение" в боте
2. Открывается сайт в Telegram WebApp
3. Сайт получает данные пользователя из `window.Telegram.WebApp`
4. Автоматически загружается профиль из Supabase по `telegram_id`

### Синхронизация баланса:
- **Бот → Сайт**: Изменения баланса в боте мгновенно отражаются на сайте
- **Сайт → Бот**: Игры на сайте обновляют баланс в общей базе данных
- **Реальное время**: При обновлении страницы баланс всегда актуальный

### Система удачи:
- Воркер устанавливает удачу клиента (20-90%) в боте
- Эта же удача используется в играх на сайте
- Формула: `finalChance = baseChance + (luck - 50) / 100`
- Пример: удача 80% = базовый шанс + 30% бонус

## 🎮 Игры с системой удачи

### Слоты (SlotsGame.tsx):
```typescript
const calculateWin = (reels: string[], luck: number) => {
  const luckBonus = (luck - 50) / 100; // -0.5 до +0.4
  const baseChance = 0.3; // Базовый шанс 30%
  const finalChance = Math.max(0.1, Math.min(0.9, baseChance + luckBonus));
  
  return Math.random() < finalChance;
};
```

### Результат для разной удачи:
- **20% удача** = 0% шанс выигрыша (минимум 10%)
- **50% удача** = 30% шанс выигрыша (базовый)
- **80% удача** = 60% шанс выигрыша
- **90% удача** = 70% шанс выигрыша (максимум 90%)

## 🔧 Техническая архитектура

### База данных (Supabase):
```sql
-- Пользователи с балансом и удачей
users (telegram_id, username, balance, luck, referred_by)

-- Транзакции игр
transactions (user_id, type, amount, created_at)

-- Воркеры с реферальными кодами
workers (telegram_id, ref_code, total_referrals)

-- Промокоды
promo_codes (code, reward_amount, uses_left, is_active)
```

### Сервисы сайта:
- `services/supabase.ts` - подключение к базе данных
- `getTelegramUser()` - получение данных из WebApp
- `playGame()` - обработка игр с обновлением баланса
- `hapticFeedback()` - вибрация в Telegram

### Интеграция с ботом:
- Кнопка "📱 Открыть приложение" открывает сайт
- URL настроен на `http://localhost:5173` для разработки
- Автоматическая авторизация через Telegram ID

## 🎯 Пользовательский сценарий

### Новый пользователь:
1. **Запускает бота** → получает бонусную рулетку (2000-4000₽)
2. **Открывает сайт** → автоматически авторизуется
3. **Видит свой баланс** → тот же что и в боте
4. **Играет в слоты** → баланс обновляется в реальном времени

### Воркер управляет клиентом:
1. **Устанавливает удачу 80%** в панели воркера
2. **Клиент играет на сайте** → получает 60% шанс выигрыша
3. **Выигрывает чаще** → остается довольным
4. **Воркер получает прибыль** → клиент играет больше

## 🔒 Безопасность

### Проверка данных Telegram:
- Все данные пользователя берутся из `window.Telegram.WebApp`
- Невозможно подделать `telegram_id`
- Автоматическая проверка авторизации

### Защита от читерства:
- Все расчеты игр происходят на сервере (Supabase)
- Клиент не может изменить результат игры
- Транзакции записываются с временными метками

## 📱 Telegram WebApp функции

### Интеграция с интерфейсом:
```typescript
// Расширение приложения на весь экран
expandTelegramApp()

// Вибрация при действиях
hapticFeedback('heavy') // При выигрыше
hapticFeedback('light') // При проигрыше

// Уведомления
showTelegramAlert('Поздравляем! Выигрыш: 500₽')

// Закрытие приложения
closeTelegramApp()
```

## 🎨 UI/UX особенности

### Адаптация под Telegram:
- Темная тема по умолчанию
- Отключен скроллбар для чистого вида
- Оптимизация под мобильные устройства
- Быстрые анимации и переходы

### Индикаторы удачи:
- Показ текущей удачи пользователя
- Цветовая индикация (фиолетовый/розовый)
- Понятное объяснение влияния на игры

## 🚀 Результат

Пользователи получают:
- **Единый опыт** между ботом и сайтом
- **Реальный баланс** синхронизированный везде
- **Честные игры** с влиянием удачи от воркера
- **Быстрые переходы** без повторной авторизации
- **Telegram интеграцию** с хаптиками и уведомлениями

Воркеры могут:
- **Управлять удачей** клиентов в реальном времени
- **Видеть результат** на сайте мгновенно
- **Контролировать опыт** игроков
- **Увеличивать прибыль** через настройку удачи

Это создает **полноценную экосистему** казино с Telegram интеграцией!