# 🌐 Настройка ngrok для Telegram WebApp

## 🎯 Проблема
Telegram WebApp требует HTTPS URL для работы. Локальный сервер `http://localhost:3000` не подходит.

## 🚀 Решение с ngrok

### 1. Установка ngrok
```bash
# macOS
brew install ngrok

# Windows
# Скачать с https://ngrok.com/download
```

### 2. Запуск туннеля
```bash
ngrok http 3000
```

### 3. Получение URL
После запуска ngrok покажет URL вида:
```
https://5b99763cf7b8.ngrok-free.app -> http://localhost:3000
```

### 4. Обновление конфигурации

#### В `bots/casino/.env`:
```env
WEBSITE_URL=https://5b99763cf7b8.ngrok-free.app
```

#### В `bots/casino/config.py`:
```python
WEBSITE_URL = os.getenv("WEBSITE_URL", "https://5b99763cf7b8.ngrok-free.app")
```

## ⚙️ Настройка Vite

### В `royal-fortune-casino/vite.config.ts`:
```typescript
export default defineConfig(({ mode }) => {
    return {
      server: {
        port: 3000,
        host: '0.0.0.0',
        allowedHosts: 'all', // Разрешить все хосты для ngrok
      },
      // ...
    };
});
```

## 🔄 Процесс запуска

### 1. Запуск сайта:
```bash
cd royal-fortune-casino
npm run dev
```

### 2. Запуск ngrok (в новом терминале):
```bash
ngrok http 3000
```

### 3. Обновление URL в боте:
- Скопировать HTTPS URL из ngrok
- Обновить `WEBSITE_URL` в `.env`
- Перезапустить бота

### 4. Запуск бота:
```bash
cd bots/casino
python main.py
```

## ✅ Результат

Теперь кнопка "📱 Открыть приложение" в боте будет открывать сайт через HTTPS туннель ngrok, что позволит:

- ✅ **Работать в Telegram WebApp**
- ✅ **Получать данные пользователя**
- ✅ **Использовать хаптики и уведомления**
- ✅ **Синхронизировать баланс с ботом**

## 🔧 Альтернативы ngrok

### Другие туннели:
- **Cloudflare Tunnel** (бесплатный)
- **LocalTunnel** (бесплатный)
- **Serveo** (бесплатный)

### Продакшн:
- Деплой на **Vercel**, **Netlify**, **Railway**
- Использование собственного домена с SSL

## 🚨 Важные моменты

### Безопасность:
- ngrok URL временный и меняется при перезапуске
- Для продакшна использовать постоянный домен
- Не делиться ngrok URL публично

### Обновление URL:
- При каждом перезапуске ngrok URL меняется
- Нужно обновлять конфигурацию бота
- Можно использовать платный ngrok для постоянного URL

## 📱 Тестирование

1. Запустить бота в Telegram
2. Нажать "📱 Открыть приложение"
3. Сайт должен открыться в WebApp
4. Проверить авторизацию и баланс
5. Протестировать игры с синхронизацией